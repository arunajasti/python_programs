
# strip() removes the spaces Before & After of string
str1 = " Aruna is a Good girl "
print(len(str1))

print(str1.strip())

b= str1.strip() # to find the len of str with spaces to assign the strip() to var
print(len(b))
